import React, { Component } from "react";
import MediaElement from "./MediaElement";

class Gallery extends Component {
  render() {
    return (
      <div>
        <MediaElement time={} author={} title={} imageNumber={} />
      </div>
    );
  }
}

export default Gallery;
